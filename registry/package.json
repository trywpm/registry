{
  "name": "@wpm/registry",
  "private": true,
  "scripts": {
    "start": "vp run typegen && wrangler dev",
    "typegen": "wrangler types worker-types.d.ts --env-file .env.example"
  },
  "dependencies": {
    "@wpm/db": "workspace:*",
    "@wpm/manifest": "workspace:*",
    "@wpm/middleware": "workspace:*",
    "@wpm/rbac": "workspace:*",
    "@wpm/util": "workspace:*",
    "hono": "catalog:",
    "postgres": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
