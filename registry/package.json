{
  "name": "@wpm/registry",
  "private": true,
  "scripts": {
    "start": "vp run typegen && wrangler dev",
    "typegen": "wrangler types worker-types.d.ts --env-file .env.example"
  },
  "dependencies": {
    "@wpm/manifest": "workspace:*",
    "@wpm/rbac": "workspace:*",
    "@wpm/util": "workspace:*",
    "hono": "catalog:",
    "postgres": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
